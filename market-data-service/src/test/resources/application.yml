spring:
  application:
    name: market-data-service-test
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  cache:
    type: simple

# Disable WebSocket message broker in tests by providing minimal config
  main:
    web-application-type: servlet

eureka:
  client:
    enabled: false

market:
  alpha-vantage:
    api-key: demo
    base-url: https://www.alphavantage.co

resilience4j:
  circuitbreaker:
    instances:
      alphaVantage:
        slidingWindowSize: 5
        failureRateThreshold: 100
        waitDurationInOpenState: 1s
  retry:
    instances:
      alphaVantage:
        maxAttempts: 1

logging:
  level:
    com.stockiq: WARN

# Disable scheduling in tests to avoid SimpMessagingTemplate startup issues
task:
  scheduling:
    enabled: false
